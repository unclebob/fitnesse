This page requires that 3 varaibles be defined before it is included.
 * NUMERATOR
 * DENOMINATOR
 * QUOTIENT

!|eg.Division|
|numerator|denominator|quotient?|
|${NUMERATOR}|${DENOMINATOR}|${QUOTIENT}|
