<ivysettings>
        <settings defaultResolver="default" />
        <resolvers>
		<chain name="default">
			<ibiblio name="maven2" root="${maven.central.url}" m2compatible="true" />
		</chain>
        </resolvers>
</ivysettings>
